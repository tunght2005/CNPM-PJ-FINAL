from django.apps import AppConfig

class NotifiServiceConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'Notifi_Service'

# Form có sẳn
# from django.apps import AppConfig

# class NotifiServiceConfig(AppConfig):
#     default_auto_field = 'django.db.models.BigAutoField'
#     name = 'Notifi_Service'
